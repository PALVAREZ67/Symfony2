{% extends "base.html.twig" %}

{% block body %}
    
    <section class="new-program">

        {{ form_start(form) }}
            <div class="">
                {{ form_errors(form) }}
            </div>

            {{ form_widget(form) }}

            <div class="">
                <button class="btn-form" type="submit">Add program</button>
            </div>
            
        {{ form_end(form) }}
        {% if test is not empty %} {{dump(test)}} {% endif %}
    </section>



{% endblock %}